import React from 'react';
import { Button } from 'react-bootstrap';

const ButtonConfirmChange = () => {
  return (
    <Button variant="primary" aria-label="Confirmar Alteração">
      Confirmar Alteração
    </Button>
  );
};

export default ButtonConfirmChange;
